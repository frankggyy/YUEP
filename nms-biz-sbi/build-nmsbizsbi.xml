<?xml version="1.0"?>

<project name="nmsbizsbi" default="deploy" basedir=".">
	<property name="PrjHome" value="." />
	<property name="BuildProject" value="yuep-build" />
	<import file="${PrjHome}/../${BuildProject}/support/properties.xml" />

	<property name="FmSbi.pkg" value="fmsbi.jar" />
	<property name="PmSbi.pkg" value="pmsbi.jar" />
	<property name="MoExtSbi.pkg" value="moextsbi.jar" />
	<property name="DiscoverySbi.pkg" value="discoverysbi.jar" />
	<property name="EqmSbi.pkg" value="eqmsbi.jar" />

	<property name="FmSbiModule" value="fm-sbi" />
	<property name="PmSbiModule" value="pm-sbi" />
	<property name="MoExtSbiModule" value="moext-sbi" />
	<property name="DiscoverySbiModule" value="discovery-sbi" />
	<property name="EqmSbiModule" value="eqm-sbi" />

	<property name="dest" value="${PrjHome}/nms-biz-sbi-classes" />
	<property name="deploy" value="${PrjHome}/lib/${product}" />
	<property name="NmsBizSbiSrc" value="${PrjHome}/src/java" />
	<property name="NmsBizSbiSrcResc" value="${PrjHome}/resource" />

	<target name="mkdir">
		<mkdir dir="${dest}" />
		<mkdir dir="${deploy}" />
	</target>

	<target depends="mkdir" name="make">
		<javac bootclasspathref="project.class.path" encoding="${javac.encoding}" debug="${javac.debug}" debuglevel="${javac.debuglevel}" optimize="${javac.optimize}" deprecation="${javac.deprecation}" nowarn="${javac.nowarn}" source="${javac.source}" target="${javac.target}" listfiles="${javac.listfiles}" fork="${javac.fork}" includeAntRuntime="${javac.includeAntRuntime}" includeJavaRuntime="${javac.includeJavaRuntime}" destdir="${dest}" includes="${nms.biz.sbi.prefix}/**">

			<src path="${NmsBizSbiSrc}" />
			<compilerarg value="-J-Duser.language=en" />
		</javac>
	</target>

	<target name="clean">
		<delete failonerror="false" includeemptydirs="true">
			<fileset dir="${deploy}" />
		</delete>
		<delete failonerror="false" includeemptydirs="true">
			<fileset dir="${dest}" />
		</delete>
	</target>

	<target name="copy_src_conf_file">
		<copy todir="${dest}">
			<fileset dir="${NmsBizSbiSrc}/">
				<include name="**/*.*" />
				<exclude name="**/*.java" />
			</fileset>
		</copy>
	</target>

	<target name="jar1package">
		<jar basedir="${dest}" index="${manifest.index}" duplicate="${manifest.duplicate}" destfile="${deploy}/${jarname}" includes="${nms.biz.sbi.prefix}/${packagename}/**">
			<manifest>
				<attribute name="Vendor" value="${manifest.vendor}" />
				<attribute name="Built-Time" value="${LOCALTIME}" />
				<attribute name="Version" value="${manifest.version}" />
				<attribute name="BuildNumber" value="${manifest.buildnumber}" />
			</manifest>
		</jar>
	</target>

	<target depends="clean,make,copy_src_conf_file" name="jar">
		<antcall target="jar1package">
			<param name="jarname" value="${EqmSbi.pkg}" />
			<param name="packagename" value="eqm" />
		</antcall>
	</target>

	<target name="copy_files">
		<copy tofile="${runenv.dir}/modules/${EqmSbiModule}/lib/${EqmSbi.pkg}" file="${deploy}/${EqmSbi.pkg}" overwrite="true" />
		<copy todir="${runenv.dir}/modules/${EqmSbiModule}/conf">
			<fileset dir="${NmsBizSbiSrcResc}/conf">
				<include name="**/*.*" />
				<exclude name="**/*svn" />
			</fileset>
		</copy>
	</target>

	<target depends="jar,copy_files" name="deploy" />

</project>