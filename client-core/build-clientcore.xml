<?xml version="1.0"?>

<project name="clientcore" default="deploy" basedir=".">

	<property name="PrjHome" value="." />
	<property name="BuildProject" value="yuep-build" />
	<import file="${PrjHome}/../${BuildProject}/support/properties.xml" />

	<property name="ClientCore.pkg" value="clientcore.jar" />
	<property name="dest" value="${PrjHome}/client-core-classes" />
	<property name="deploy" value="${PrjHome}/lib/${product}" />
	<property name="ClientCoreSrc" value="${PrjHome}/src/java" />
	<property name="ClientCoreConf" value="${PrjHome}/resource/conf" />

	<property name="ClientCoreModule" value="clientcore" />
	<property name="ClientCoreCfg.pkg" value="clientcore-cfg.jar" />
	
	<target name="mkdir">
		<mkdir dir="${dest}" />
		<mkdir dir="${deploy}" />
	</target>

	<target depends="mkdir" name="make">
		<javac bootclasspathref="project.class.path" encoding="${javac.encoding}" 
			debug="${javac.debug}" debuglevel="${javac.debuglevel}" 
			optimize="${javac.optimize}" deprecation="${javac.deprecation}" 
			nowarn="${javac.nowarn}" source="${javac.source}" 
			target="${javac.target}" listfiles="${javac.listfiles}" 
			fork="${javac.fork}" includeAntRuntime="${javac.includeAntRuntime}" 
			includeJavaRuntime="${javac.includeJavaRuntime}" destdir="${dest}" 
			includes="${client.core.prefix}/**">

			<src path="${ClientCoreSrc}" />
			<compilerarg value="-J-Duser.language=en" />
		</javac>
	</target>

	<target name="clean">
		<delete file="${deploy}/${ClientCore.pkg}" />
		<delete failonerror="false" includeemptydirs="true">
			<fileset dir="${dest}" />
		</delete>
	</target>

	<target name="copy_src_conf_file">
		<copy todir="${dest}">
			<fileset dir="${ClientCoreSrc}/">
				<include name="**/*.*" />
				<exclude name="**/*.java" />
			</fileset>
		</copy>
	</target>

	<target depends="clean,make,copy_src_conf_file" name="jar">
		<jar basedir="${dest}" index="${manifest.index}"
			duplicate="${manifest.duplicate}" destfile="${deploy}/${ClientCore.pkg}"
			includes="${client.core.prefix}/**">
			<manifest>
				<attribute name="Vendor" value="${manifest.vendor}" />
				<attribute name="Built-Time" value="${LOCALTIME}" />
				<attribute name="Version" value="${manifest.version}" />
				<attribute name="BuildNumber" value="${manifest.buildnumber}" />
			</manifest>
		</jar>
	</target>

	<target name="jar.springcfg">
		<jar basedir="${PrjHome}/resource/" index="${manifest.index}" 
			duplicate="${manifest.duplicate}" destfile="${deploy}/${ClientCoreCfg.pkg}" 
			includes="conf/appctx*.xml">
			<manifest>
				<attribute name="Vendor" value="${manifest.vendor}" />
				<attribute name="Built-Time" value="${LOCALTIME}" />
				<attribute name="Version" value="${manifest.version}" />
				<attribute name="BuildNumber" value="${manifest.buildnumber}" />
			</manifest>
		</jar>
	</target>

	<target name="copy_files">
		<copy tofile="${runenv.dir}/modules/${ClientCoreModule}/lib/${ClientCore.pkg}" file="${deploy}/${ClientCore.pkg}" overwrite="true"/>
		<copy tofile="${runenv.dir}/modules/${ClientCoreModule}/lib/${ClientCoreCfg.pkg}" file="${deploy}/${ClientCoreCfg.pkg}" overwrite="true"/>
		<copy todir="${runenv.dir}/modules/${ClientCoreModule}/conf" overwrite="true">
			<fileset dir="${ClientCoreConf}/">
				<include name="**/*.*" />
				<exclude name="**/appctx*.xml" />
				<exclude name="**/*svn" />
			</fileset>
		</copy>
		<copy todir="${runenv.dir}/modules/${ClientCoreModule}/lib" overwrite="true">
			<fileset dir="${PrjHome}/lib/looks/">
				<include name="**/*.*" />
				<exclude name="**/*svn" />
			</fileset>
		</copy>
		<copy todir="${runenv.dir}/modules/${ClientCoreModule}/lib" overwrite="true">
			<fileset dir="${PrjHome}/lib/twaver/">
				<include name="**/*.*" />
				<exclude name="**/*svn" />
			</fileset>
		</copy>
	</target>


	<target depends="jar,jar.springcfg,copy_files" name="deploy" />

</project>