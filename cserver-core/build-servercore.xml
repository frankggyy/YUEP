<?xml version="1.0"?>

<project name="servercore" default="deploy" basedir=".">

	<property name="PrjHome" value="." />
	<property name="BuildProject" value="yuep-build" />
	<import file="${PrjHome}/../${BuildProject}/support/properties.xml" />
	
	<property name="resource" value="${PrjHome}/resource" />

	<property name="ServerCore.pkg" value="servercore.jar"/>
	<property name="Transaction.pkg" value="transaction.jar" />
	
	<property name="dest" value="${PrjHome}/server-core-classes" />
	<property name="deploy" value="${PrjHome}/lib/${product}" />
	<property name="ServerCoreSrc" value="${PrjHome}/src/java" />
	
	<property name="ServerCoreModule" value="servercore" />
	<property name="ServerCoreCfg.pkg" value="servercore-cfg.jar" />
	
	<property name="JmsServerModule" value="jmsserver" />
	<property name="JmsServer.pkg" value="jmsserver.jar" />
	
	<property name="DbModule" value="db" />
	<property name="db.pkg" value="db.jar" />
	

	<target name="mkdir">
		<mkdir dir="${dest}" />
		<mkdir dir="${deploy}" />
	</target>

	<target depends="mkdir" name="make">
		<javac bootclasspathref="project.class.path" encoding="${javac.encoding}" 
			debug="${javac.debug}" debuglevel="${javac.debuglevel}" 
			optimize="${javac.optimize}" deprecation="${javac.deprecation}" 
			nowarn="${javac.nowarn}" source="${javac.source}" 
			target="${javac.target}" listfiles="${javac.listfiles}" 
			fork="${javac.fork}" includeAntRuntime="${javac.includeAntRuntime}" 
			includeJavaRuntime="${javac.includeJavaRuntime}" destdir="${dest}" 
			includes="${server.core.prefix}/**">

			<src path="${ServerCoreSrc}" />
			<compilerarg value="-J-Duser.language=en" />
		</javac>
	</target>

	<target name="clean">
		<delete failonerror="false" includeemptydirs="true">
			<fileset dir="${deploy}" />
		</delete>
		<delete failonerror="false" includeemptydirs="true">
			<fileset dir="${dest}" />
		</delete>
	</target>

	<target name="copy_src_conf_file">
		<copy todir="${dest}">
			<fileset dir="${ServerCoreSrc}/">
				<include name="**/*.*" />
				<exclude name="**/*.java" />
			</fileset>
		</copy>
	</target>

	<target name="jar1package">
		<jar basedir="${dest}" index="${manifest.index}"
			duplicate="${manifest.duplicate}" destfile="${deploy}/${jarname}"
			includes="${server.core.prefix}/${packagename}/**">
			<manifest>
				<attribute name="Vendor" value="${manifest.vendor}" />
				<attribute name="Built-Time" value="${LOCALTIME}" />
				<attribute name="Version" value="${manifest.version}" />
				<attribute name="BuildNumber" value="${manifest.buildnumber}" />
			</manifest>
		</jar>
		
		<condition property="havespringcfg">
			<isset property="cfgjarname"/> 
		</condition>
		<antcall target="jarspringcfg">  
			<param name="packagename" value="${packagename}" />
			<param name="cfgjarname" value="${cfgjarname}" />
		</antcall> 
	</target>
	
	<target name="jarspringcfg" if="havespringcfg">
		<jar basedir="${PrjHome}/resource/${packagename}" index="${manifest.index}"
			duplicate="${manifest.duplicate}" destfile="${deploy}/${cfgjarname}">
			<manifest>
				<attribute name="Vendor" value="${manifest.vendor}" />
				<attribute name="Built-Time" value="${LOCALTIME}" />
				<attribute name="Version" value="${manifest.version}" />
				<attribute name="BuildNumber" value="${manifest.buildnumber}" />
			</manifest>
		</jar>
	</target>

	<target depends="clean,make,copy_src_conf_file" name="jar">
		<antcall target="jar1package">
			<param name="jarname" value="${db.pkg}" />
			<param name="packagename" value="${DbModule}" />
			<param name="cfgjarname" value="dbcfg.jar" />
		</antcall>
		<antcall target="jar1package">
			<param name="jarname" value="${JmsServer.pkg}" />
			<param name="packagename" value="${JmsServerModule}" />
		</antcall>
		<antcall target="jar1package">
			<param name="jarname" value="${ServerCore.pkg}" />
			<param name="packagename" value="servercore" />
			<param name="cfgjarname" value="${ServerCoreCfg.pkg}" />
		</antcall>
	</target>

	<target name="copy_files">
		<copy tofile="${runenv.dir}/modules/${DbModule}/lib/${db.pkg}" file="${deploy}/${db.pkg}" overwrite="true"/>
		<copy todir="${runenv.dir}/modules/${DbModule}/" overwrite="true">
			 <fileset dir="${resource}/${DbModule}">
			 </fileset>
		</copy>
		<copy tofile="${runenv.dir}/modules/${JmsServerModule}/lib/${JmsServer.pkg}" file="${deploy}/${JmsServer.pkg}" overwrite="true"/>
		<copy tofile="${runenv.dir}/modules/${ServerCoreModule}/lib/${ServerCore.pkg}" file="${deploy}/${ServerCore.pkg}" overwrite="true"/>
		<copy tofile="${runenv.dir}/modules/${ServerCoreModule}/lib/${ServerCoreCfg.pkg}" file="${deploy}/${ServerCoreCfg.pkg}" overwrite="true"/>
		<copy tofile="${runenv.dir}/modules/${ServerCoreModule}/conf/log4j-3rdparty.properties" file="${PrjHome}/resource/${ServerCoreModule}/conf/log4j-3rdparty.properties" overwrite="true"/>
	</target>
			
	<target depends="jar,copy_files" name="deploy" />

</project>