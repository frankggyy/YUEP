<?xml version="1.0"?>

<project name="nmscoresbi" default="deploy" basedir=".">
	<property name="PrjHome" value="." />
	<property name="BuildProject" value="yuep-build" />
	<import file="${PrjHome}/../${BuildProject}/support/properties.xml" />

	<property name="SbiCore.pkg" value="sbicore.jar" />
	<property name="dest" value="${PrjHome}/sbi-core-classes" />
	<property name="deploy" value="${PrjHome}/lib/${product}" />
	<property name="SbiCoreSrc" value="${PrjHome}/src/java" />

	<target name="mkdir">
		<mkdir dir="${dest}" />
		<mkdir dir="${deploy}" />
	</target>

	<target depends="mkdir" name="make">
		<javac bootclasspathref="project.class.path" encoding="${javac.encoding}" 
			debug="${javac.debug}" debuglevel="${javac.debuglevel}" 
			optimize="${javac.optimize}" deprecation="${javac.deprecation}" 
			nowarn="${javac.nowarn}" source="${javac.source}" 
			target="${javac.target}" listfiles="${javac.listfiles}" 
			fork="${javac.fork}" includeAntRuntime="${javac.includeAntRuntime}" 
			includeJavaRuntime="${javac.includeJavaRuntime}" destdir="${dest}" 
			includes="${nms.core.sbi.prefix}/**">

			<src path="${SbiCoreSrc}" />
			<compilerarg value="-J-Duser.language=en" />
		</javac>
	</target>

	<target name="clean">
		<delete failonerror="false" includeemptydirs="true">
			<fileset dir="${deploy}" />
		</delete>
		<delete failonerror="false" includeemptydirs="true">
			<fileset dir="${dest}" />
		</delete>
	</target>

	<target name="copy_src_conf_file">
		<copy todir="${dest}">
			<fileset dir="${SbiCoreSrc}/">
				<include name="**/*.*" />
				<exclude name="**/*.java" />
			</fileset>
		</copy>
	</target>

	<target depends="clean,make,copy_src_conf_file" name="jar">
		<jar basedir="${dest}" index="${manifest.index}"
				duplicate="${manifest.duplicate}" destfile="${deploy}/${SbiCore.pkg}"
				includes="${nms.core.sbi.prefix}/**">
			<manifest>
				<attribute name="Vendor" value="${manifest.vendor}" />
				<attribute name="Built-Time" value="${LOCALTIME}" />
				<attribute name="Version" value="${manifest.version}" />
				<attribute name="BuildNumber" value="${manifest.buildnumber}" />
			</manifest>
		</jar>
		<jar basedir="${PrjHome}/resource" index="${manifest.index}"
				duplicate="${manifest.duplicate}" destfile="${deploy}/sbicorecfg.jar"
				includes="conf/appctx*.xml">
			<manifest>
				<attribute name="Vendor" value="${manifest.vendor}" />
				<attribute name="Built-Time" value="${LOCALTIME}" />
				<attribute name="Version" value="${manifest.version}" />
				<attribute name="BuildNumber" value="${manifest.buildnumber}" />
			</manifest>
		</jar>
	</target>

	<target name="copy_files">
		<copy tofile="${runenv.dir}/modules/sbicore/lib/sbicorecfg.jar" file="${deploy}/sbicorecfg.jar" overwrite="true"/>
		<copy tofile="${runenv.dir}/modules/sbicore/lib/${SbiCore.pkg}" file="${deploy}/${SbiCore.pkg}" overwrite="true"/>
		<copy todir="${runenv.dir}/modules/sbicore/conf" overwrite="true">
			<fileset dir="${PrjHome}/resource/conf">
				<include name="*.xml" />
				<exclude name="appctx*.xml" />
			</fileset>
		</copy>
	</target>

	<target depends="jar,copy_files" name="deploy" />

</project>